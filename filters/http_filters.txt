# Basic HTTP traffic
http

# HTTP over standard port
tcp.port == 80

# Show only POST requests
http.request.method == "POST"

# Show only GET requests
http.request.method == "GET"

# Filter traffic between client and server
ip.addr == 10.6.13.133 && ip.addr == 10.6.13.129
