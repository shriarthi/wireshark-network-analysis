# Display all HTTP traffic
http

# Show only HTTP POST requests
http.request.method == "POST"

# Show only HTTP GET requests
http.request.method == "GET"

# Filter traffic to the server IP
ip.addr == 10.6.13.129

# Filter traffic between client and server
ip.src == 10.6.13.133 && ip.dst == 10.6.13.129
